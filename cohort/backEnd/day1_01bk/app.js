const express = require("express");

const app = express(); //server is created only create kiya hai chalaya nhi hai
app.listen(3000); //server ko start kerta hai or ye 3000 jo pai ye port number hai is port per start hua hai
