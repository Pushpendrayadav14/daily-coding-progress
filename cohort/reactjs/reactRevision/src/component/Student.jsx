import React from 'react'

export const Student = ({name,age,collage}) => {
  return (
    <div>
        <h1>Student name : {name}</h1>
        <h1>Student age: {age}</h1>
        <h1>Student collage: {collage}</h1>
    </div>
  )
}
